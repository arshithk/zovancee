{
  "name": "zovance-website",
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build": "npx vite build",
    "lint:css": "stylelint \"src/**/*.css\"",
    "lint:js": "eslint \"src/**/*.js\""
  },
  "dependencies": {
    "vite": "^5.0.0"
  },
  "devDependencies": {
    "stylelint": "^15.0.0",
    "eslint": "^8.0.0"
  }
}